%h2 Recently added
.row.entries.dashboard
  - Entry.types.each do |type|
    .span3
      %h3= type.to_s.capitalize
      = render partial: "dashboard/entries", locals: { entries: @recent[type.to_s.pluralize.to_sym] }
